log_interval: 10
save_interval: 1000
checkpoint_dir: "./checkpoints"

# Logging backends configuration
tensorboard:
  enabled: true
  log_dir: "runs/${run_name}"

json:
  enabled: false
  log_file: "logs/${run_name}.json"